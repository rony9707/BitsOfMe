import { PostsFormatTextPipe } from './posts-format-text.pipe';

describe('PostsFormatTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PostsFormatTextPipe();
    expect(pipe).toBeTruthy();
  });
});
