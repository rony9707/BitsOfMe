import { LoaderButtonDirectiveDirective } from './loaderButton-directive.directive';

describe('LoaderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LoaderButtonDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
