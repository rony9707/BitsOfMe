import { LoaderDirectiveDirective } from './loaderButton-directive.directive';

describe('LoaderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LoaderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
