import { HideScrollDirective } from './hide-scroll.directive';

describe('HideScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HideScrollDirective();
    expect(directive).toBeTruthy();
  });
});
